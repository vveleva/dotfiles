# Path to your oh-my-zsh installation
export ZSH=$HOME/.oh-my-zsh

ZSH_THEME="velina"

# CASE_SENSITIVE="true" # use case-sensitive completion
# DISABLE_AUTO_UPDATE="true" # disable bi-weekly auto-update checks
# export UPDATE_ZSH_DAYS=13 # change how often to auto-update (in days)
# DISABLE_LS_COLORS="true" # disable colors in ls
# DISABLE_AUTO_TITLE="true" #  disable auto-setting terminal title
# DISABLE_CORRECTION="true" # disable command auto-correction
# COMPLETION_WAITING_DOTS="true" # display red dots while waiting for completion
# DISABLE_UNTRACKED_FILES_DIRTY="true" # disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# HIST_STAMPS="mm/dd/yyyy" # change the timestamp in the history command output
# ZSH_CUSTOM=/path/to/new-custom-folder # use custom folder than $ZSH/custom?

##### ZSH PLUGINS ##############################################################
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/

# plugins=(git ruby osx gem django zsh-syntax-highlighting.zsh history history-substring-search.zsh terminalapp brew zsh-syntax-highlighting colored-man colorize cp nyan rand-quote)

# source ~/.zsh/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

source $ZSH/oh-my-zsh.sh

# zsh-syntax-highlighting
ZSH_HIGHLIGHT_HIGHLIGHTERS=(main brackets pattern cursor)
# ZSH_HIGHLIGHT_STYLES[path]='fg=cyan'
# ZSH_HIGHLIGHT_STYLES[cursor]='bg=blue'


##### AUTOLOAD ZSH FUNCTIONS ###################################################
fpath=(~/.zsh/functions $fpath)
autoload -U ~/.zsh/functions/*(:t)

